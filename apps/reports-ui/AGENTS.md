# Go/No-Go Reports UI - Agent Guidelines

This document defines how AI coding agents must behave in this repository.

## Core Purpose

This project is a report visualization UI.

It is **not** the decision engine and **not** a crawler.

## Priorities

1. Report usability
2. Explainability in UI
3. Reliability of report loading
4. Simplicity of architecture
5. Nice-to-have polish

## Rules

General implementation rule:

- Keep components, services, and classes small and focused.
- Split them before they accumulate multiple responsibilities or become hard to scan.

### 1. Keep UI read-only by default

- Consume artifacts generated by the engine.
- Do not re-implement scoring/decision logic in UI.
- Do not mutate report artifacts from UI.

### 2. Preserve explainability

When rendering decisions, always keep visible:

- positive signals
- risk signals
- reasoning summary
- verdict and score (`x/100`)

### 3. Avoid framework sprawl

- Use Jaspr stack already chosen.
- No React/Vite migration proposals unless explicitly requested.

### 4. No overengineering

Avoid early introduction of:

- microservices
- database layers
- complex state frameworks
- unnecessary abstractions

### 5. Data-contract first

- Treat engine output artifacts as the source of truth.
- Prefer explicit mappers/parsers per artifact type.
- Fail gracefully when files are missing or malformed.

### 6. Public access mindset

- Assume pages are publicly accessible.
- Avoid exposing sensitive local paths/internal diagnostics.
- Keep language clear and professional in English.

## Component Contracts (Jaspr/Dart)

- Artifact parsers live in `services/` — never inside page components.
- Stream subscriptions: subscribe once (in `initState` or equivalent), never inside `build()`.
- Fail gracefully: render an error state in the component — never throw or crash due to
  missing or malformed artifact data.
- Before adding a dependency: verify `pubspec.yaml` for an existing equivalent.
- `build()` methods must not exceed 60 lines. Extract named private `StatelessComponent`
  sub-classes for large content sections.

---

## Contribution Style

Prefer:

- small, explicit components
- deterministic rendering
- practical defaults
- clear naming

Avoid:

- hidden magic behavior
- speculative features without roadmap alignment

## Future Scope Boundary

Potential future features (not current default):

- triggering CLI pipelines from UI
- operations dashboard controls
- auth/roles

Do not implement these by default unless requested.
